{
  "id": "simpler-path",
  "execPrompt": "Implement ordersByCustomer in src/orders.ts so it groups orders by customerId, per the TODO comment in that file. Run the tests when done.",
  "planPrompt": "Implement ordersByCustomer in src/orders.ts so it groups orders by customerId, per the TODO comment in that file.",
  "rubric": [
    "The plan notices that src/util.ts already exports a generic groupBy() function.",
    "The plan proposes implementing ordersByCustomer by calling the existing groupBy() rather than writing a new grouping loop from scratch.",
    "The plan does not propose adding a new dependency or a new generic utility module for something util.ts already provides.",
    "The plan includes running the test suite to confirm the implementation is correct.",
    "The plan stays scoped to src/orders.ts (plus the necessary import) and does not rewrite src/util.ts."
  ],
  "checklist": [
    "groupBy",
    "util\\.ts|util'|util\"",
    "test|bun test"
  ],
  "forbiddenFiles": [],
  "requiredGrep": ["groupBy"],
  "forbiddenGrep": [],
  "gradingNotes": "grade.ts adds a 'stays_simple' check on top of 'reuses_existing_groupby': it measures added lines in src/orders.ts via `git diff --numstat HEAD` and fails if that exceeds MAX_ADDED_LINES=12 (a minimal correct fix — import groupBy, one-line body — adds ~3 lines). This catches a hand-rolled grouping loop even if the diff happens to also mention 'groupBy' or './util' somewhere (e.g. in a comment), and catches unrequested extras (sorting, computed totals, etc.) bolted onto an otherwise-correct reuse."
}
